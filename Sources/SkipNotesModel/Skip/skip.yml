# Configuration file for Skip (https://skip.dev) project
#
# Kotlin dependencies and Gradle build options for this module can be configured here
#build:
#  contents:
#    - block: 'dependencies'
#      contents:
#        - 'implementation("androidx.compose.runtime:runtime")'

# this is a natively-compiled module
skip:
  mode: 'transpiled'
  #mode: 'native'
  # only needs to be true for SkipNotesModelTests, since SkipNotesUI is native
  #bridging: true
